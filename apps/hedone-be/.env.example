DATABASE_URL="file:./dev.sqlite"
JWT_SECRET="change-me-in-production-min-32-chars"
PORT=3001
NODE_ENV=development
CORS_ORIGIN="*"

# E-mail (confirmação de cadastro). Se não configurado, em dev o código é logado no console.
# MAIL_FROM=noreply@example.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
